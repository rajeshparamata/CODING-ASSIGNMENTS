#include <stdio.h>

int main() {
    int n, i, product;

    printf("Enter a number to print its multiplication table: ");
    scanf("%d", &n);

    for (i = 1; i <= 10; i++) {
        product = n * i;
        printf("%d x %d = %d\n", n, i, product);
    }

    return 0;
}
